# 深度学习选股系统依赖
# 安装方式:
#   pip install -r requirements_deep.txt
#
# 注意: PyTorch 需根据 CUDA 版本选择，参考 https://pytorch.org/get-started/locally/
# 以下为 CUDA 11.8 版本，其他版本请自行调整

# ===================== 核心深度学习框架 =====================
# PyTorch (CUDA 11.8)
# 请根据实际 CUDA 版本安装，命令示例:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0

# ===================== Phase 1: 序列模型 =====================
einops>=0.6.0          # 张量操作工具

# ===================== Phase 2: 图神经网络 =====================
# PyTorch Geometric (需先安装 PyTorch)
# 安装命令:
# pip install torch-geometric
# pip install torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.0.0+cu118.html
torch-geometric>=2.4.0
# torch-scatter>=2.1.0  # 按需安装
# torch-sparse>=0.6.17  # 按需安装

# ===================== Phase 5: 强化学习 =====================
stable-baselines3>=2.0.0
gymnasium>=0.29.0
# sb3-contrib>=2.0.0   # 可选，额外RL算法

# ===================== 实验跟踪 =====================
wandb>=0.15.0          # 实验记录（可选）
tensorboard>=2.14.0    # TensorBoard（可选）

# ===================== 数据处理 =====================
scipy>=1.10.0          # 科学计算
tqdm>=4.65.0           # 进度条

# ===================== 其他工具 =====================
pyyaml>=6.0            # YAML 配置解析
joblib>=1.3.0          # 模型保存
matplotlib>=3.7.0      # 可视化

# ===================== 已在主项目 requirements.txt 中的依赖 =====================
# 以下依赖假设已安装，无需重复安装：
# pandas>=1.5.0
# numpy>=1.24.0
# scikit-learn>=1.2.0
# akshare>=1.10.0
# xgboost>=1.7.0
# lightgbm>=3.3.0
# pyarrow>=12.0.0      # parquet 支持
